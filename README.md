# climate-tracker

Simple Flask app to record and visualise temperature readings by city.

## Run

1. Create a virtual environment: `python -m venv venv`
2. Activate it and install dependencies (Flask): `pip install flask`
3. Run: `python app.py`
